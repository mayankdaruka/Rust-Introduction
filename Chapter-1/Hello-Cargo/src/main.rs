// cargo new "Hello-Cargo" - create a project
// cargo build - builds a project
// ./target/debug/<binary_name> - run executable produced during build
// cargo run - builds and runs project in one go
// cargo check - build a project without producing a binary to check for errors

fn main() {
    println!("Hello, world!");
}
